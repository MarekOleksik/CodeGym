1. Initialize the variable Statics.FILE_NAME to the full path to a data file that contains several lines.
2. Then, in the static block, read all the lines from the file named Statics.FILE_NAME, and add them separately to the List lines.

Wymagania:
The constant FILE_NAME must not be empty.
In the static block, all lines from the file named FILE_NAME must be added separately to the List lines.
The field FILE_NAME must NOT be final.
The Solution class must have a list of lines.