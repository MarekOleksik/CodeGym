1. In the printMsg method, assign the current thread to the variable t.
2. In the printMsg method, add a delay of 1 millisecond after all the actions.

Wymagania:
•	The printMsg method must get the current thread using .
•	The printMsg method should put the thread to sleep for 1 millisecond.
•	The printMsg method should call the getName method on the current thread.
•	The main method should call the printMsg method on the ThreadNamePrinter object 5 times.
•	The run method must call the printMsg method 5 times.
•	The ThreadNamePrinter class's printMsg method must be called a total 10 times.