1. Figure out what the program does.
2. Implement the printTickTock method:
2.1. In the first half of each second, the following phrase should be displayed: Tick
2.2. In the second half of each second, the following phrase should be displayed: Tock

Wymagania:
•	The printTickTock method should display two lines: "Tick" and "Tock".
•	The printTickTock method should run for about a second.
•	The printTickTock method should sleep twice for half a second. Use Thread.sleep(500).
•	There should be no messages in the console after "Double-check".
•	The main method must create a Clock object.